apiVersion: v1
kind: Service
metadata:
  name: usersapi
  namespace: fiapcloudgames
spec:
  selector:
    app: usersapi
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 8080
  type: LoadBalancer
